let arr = [87, 3, 4, 0, 1, 4, 5];
let parable = [];
let strArr = arr.join(",");
console.log(strArr);
let topParable = Math.max(strArr);
console.log(topParable);
for (let i = 0; i < arr.length; i++) {}

//отсортирповать массив в порядке убывания
// макс элемент поставить на средний индекс
// расставлять элементы вокруг макс элемента по убыванию

